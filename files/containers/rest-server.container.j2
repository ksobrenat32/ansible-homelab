[Unit]
Description=Restic server container

[Container]
ContainerName=rest-server
Image=docker.io/restic/rest-server:latest
AutoUpdate=registry
PublishPort={{ rest_server_port }}:8000
Environment=OPTIONS="{{ rest_server_options }}"
Volume={{ rest_server_data_dir }}:/data:z

[Install]
WantedBy=default.target
